spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3309/pizzariacursodanki?createDatabaseIfNotExist=true
    username: root
    password: root

  jpa:
    show-sql: true

server:
  error:
    include-stacktrace: never